type Mutation {
  MakeReservation(input: ReservationInput!): Reservation!
}

type Reservation {
    id: String!
}

input ReservationInput {
    id: String!
}

type Query {
  Reservations: Reservation!
}
